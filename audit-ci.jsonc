{
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  // audit-ci supports reading JSON, JSONC, and JSON5 config files.
  // Only use one of ["low": true, "moderate": true, "high": true, "critical": true]
  "moderate": true,
  "allowlist": [ // NOTE: Please add as much information as possible to any items added to the allowList
    // sanitize-html 2.17.7 pulls htmlparser2 12.x, which is ESM-only and breaks the
    // current CommonJS/nyc coverage runtime through shins/apiDocBuilder.
    "GHSA-g8qq-57p8-ggw5|shins>sanitize-html",
    "GHSA-jxwj-j7wr-gfrw|shins>sanitize-html",
    "GHSA-vccv-cmxp-4j9h|shins>sanitize-html"
  ]
}
