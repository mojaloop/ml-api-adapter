## Add a TODO comment indicating the reason for each rejected dependency upgrade added to this list, and what should be done to resolve it (i.e. handle it through a story, etc).
reject: [
    # v6+ (ref: https://github.com/sindresorhus/get-port/releases/tag/v6.0.0) is an ESM library and thus not compatible with CommonJS. Future story needed to resolve.
    # Issue is tracked here: https://github.com/mojaloop/project/issues/3616
    "get-port",
    "commander",
    # These direct dependency bumps are outside this security override PR. Updating the Mojaloop package set together
    # currently causes npm peer dependency resolution failures around @mojaloop/event-sdk and
    # @mojaloop/central-services-logger. Handle through a separate coordinated dependency-refresh story/PR.
    "@hapi/hapi",
    "@mojaloop/central-services-error-handling",
    "@mojaloop/central-services-health",
    "@mojaloop/central-services-logger",
    "@mojaloop/central-services-metrics",
    "@mojaloop/central-services-shared",
    "@mojaloop/central-services-stream",
    "@mojaloop/database-lib",
    "@mojaloop/event-sdk",
    "@mojaloop/license-scanner-tool",
    "@mojaloop/ml-schema-transformer-lib",
    "@mojaloop/sdk-standard-components",
    "axios",
    "npm-check-updates",
    "sinon",
]
